menu "libraries"

config IFX
	bool "Interpolation Fixed point"
	default y

config FDT
	bool "flat device tree library"
	default y

config TRACE
	bool "enable the trace library"
	default y

config RINGBUFFER
	bool "simple ringbuffer library"
	default y

endmenu
